// Stub file - Backtest Manager
import * as vscode from 'vscode';
import { TRQuantClient } from './trquantClient';

export function registerBacktestManager(context: vscode.ExtensionContext, client?: TRQuantClient): void {
  // Stub implementation
}

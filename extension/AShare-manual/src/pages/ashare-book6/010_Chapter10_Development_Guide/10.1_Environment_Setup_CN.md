---
title: "10.1 环境搭建"
description: "深入解析TRQuant系统开发环境搭建，包括Python环境配置、依赖安装、数据库配置、开发工具配置等，为系统开发提供完整的环境搭建指导"
lang: "zh-CN"
layout: "/src/layouts/HandbookLayout.astro"
currentBook: "ashare-book6"
updateDate: "2025-12-12"
---

# 🛠️ 10.1 环境搭建

> **核心摘要：**
> 
> 本节系统介绍TRQuant系统的开发环境搭建，包括Python环境配置、依赖安装、数据库配置、开发工具配置等。通过理解环境搭建的完整流程，帮助开发者快速搭建开发环境，为系统开发奠定基础。

环境搭建是开发的第一步，正确的环境配置能够提高开发效率，避免常见问题。

## 📋 章节概览

<script>
function scrollToSection(sectionId) {
  const element = document.getElementById(sectionId);
  if (element) {
    const headerOffset = 100;
    const elementPosition = element.getBoundingClientRect().top;
    const offsetPosition = elementPosition + window.pageYOffset - headerOffset;
    window.scrollTo({
      top: offsetPosition,
      behavior: 'smooth'
    });
  }
}
</script>

<div class="section-overview">
  <div class="section-item" onclick="scrollToSection('section-10-1-1')">
    <h4>🐍 10.1.1 Python环境配置</h4>
    <p>Python版本、虚拟环境、包管理、环境变量</p>
  </div>
  <div class="section-item" onclick="scrollToSection('section-10-1-2')">
    <h4>📦 10.1.2 依赖安装</h4>
    <p>核心依赖、可选依赖、依赖管理、版本控制</p>
  </div>
  <div class="section-item" onclick="scrollToSection('section-10-1-3')">
    <h4>🗄️ 10.1.3 数据库配置</h4>
    <p>MongoDB、PostgreSQL、Redis、数据库连接</p>
  </div>
  <div class="section-item" onclick="scrollToSection('section-10-1-4')">
    <h4>🔧 10.1.4 开发工具配置</h4>
    <p>IDE配置、代码格式化、代码检查、测试框架</p>
  </div>
  <div class="section-item" onclick="scrollToSection('section-10-1-5')">
    <h4>✅ 10.1.5 环境验证</h4>
    <p>安装验证、功能测试、连接测试、环境检查</p>
  </div>
</div>

## 🎯 学习目标

通过本节学习，您将能够：

- **配置Python环境**：掌握Python 3.11+环境配置和虚拟环境管理
- **安装依赖**：理解核心依赖和可选依赖的安装方法
- **配置数据库**：掌握MongoDB、PostgreSQL、Redis的配置方法
- **配置开发工具**：理解IDE、代码格式化、代码检查工具的配置
- **验证环境**：掌握环境验证和功能测试方法

## 📚 核心概念

### 环境要求

- **Python版本**：Python 3.11+（必需）
- **操作系统**：Linux / macOS / Windows
- **内存**：建议 8GB+（开发环境）
- **磁盘空间**：建议 10GB+（包含数据和依赖）

### 技术栈

- **后端**：Python 3.11+、PyQt6、FastAPI
- **数据库**：MongoDB（可选）、PostgreSQL（可选）、Redis（可选）
- **数据源**：JQData、AKShare、Wind（可选）
- **开发工具**：VS Code / Cursor、pytest、black、mypy

<h2 id="section-10-1-1">🐍 10.1.1 Python环境配置</h2>

Python环境配置是开发环境的基础，需要正确配置Python版本和虚拟环境。

### Python版本要求

```bash
# 检查Python版本
python --version
# 应显示 Python 3.11.x 或更高

# 或使用python3
python3 --version
```

**要求**：Python 3.11 或更高版本

### 创建虚拟环境

```bash
# 方式1：使用venv（推荐）
python3 -m venv venv

# 激活虚拟环境
# Linux/macOS:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# 方式2：使用conda
conda create -n trquant python=3.11
conda activate trquant
```

### 验证虚拟环境

```bash
# 检查Python路径（应在虚拟环境中）
which python  # Linux/macOS
where python  # Windows

# 应显示虚拟环境路径，例如：
# /path/to/TRQuant/venv/bin/python
```

### 环境变量配置

```bash
# 创建.env文件（项目根目录）
cat > .env << EOF
# JQData配置
JQDATA_USERNAME=your_username
JQDATA_PASSWORD=your_password

# MongoDB配置（可选）
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DB=trquant

# PostgreSQL配置（可选）
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=trquant
POSTGRES_USER=trquant
POSTGRES_PASSWORD=your_password

# Redis配置（可选）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
EOF

# 或使用export（临时）
export JQDATA_USERNAME=your_username
export JQDATA_PASSWORD=your_password
```

<h2 id="section-10-1-2">📦 10.1.2 依赖安装</h2>

依赖安装包括核心依赖和可选依赖的安装。

### 核心依赖安装

```bash
# 安装核心依赖
pip install -r requirements.txt

# 或分步安装
pip install numpy>=1.24.0
pip install pandas>=2.0.0
pip install matplotlib>=3.7.0
pip install scikit-learn>=1.3.0
```

### 核心依赖列表

<CodeFromFile 
  filePath="code_library/010_Chapter10_Development_Guide/10.1/code_10_1_核心依赖列表.py"
  language="python"
  showDesignPrinciples="true"
/>

<!-- 原始代码（保留作为备份）：
```python
# requirements.txt 核心部分
# 数据处理
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# 数据源
jqdatasdk>=1.9.0  # 聚宽数据（需要账号）
akshare>=1.11.0   # 免费数据源

# GUI
PyQt6>=6.4.0
pyqtgraph>=0.13.0

# Web框架
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.31.0
```
-->

### 可选依赖安装

```bash
# MongoDB支持（可选）
pip install pymongo>=4.15.0

# PostgreSQL支持（可选）
pip install psycopg2-binary>=2.9.0

# Redis支持（可选）
pip install redis>=5.0.0

# 技术指标（可选）
pip install TA-Lib>=0.4.0

# 可视化（可选）
pip install plotly>=5.14.0
pip install seaborn>=0.12.0
```

### 依赖管理

```bash
# 导出当前环境依赖
pip freeze > requirements-lock.txt

# 安装特定版本
pip install package==version

# 升级依赖
pip install --upgrade package

# 卸载依赖
pip uninstall package
```

<h2 id="section-10-1-3">🗄️ 10.1.3 数据库配置</h2>

数据库配置包括MongoDB、PostgreSQL和Redis的配置。

### MongoDB配置（可选）

MongoDB用于数据缓存和存储，如果不需要缓存功能可以不安装。

#### Docker方式安装（推荐）

```bash
# 拉取MongoDB镜像
docker pull mongo:latest

# 运行MongoDB容器
docker run -d \
  --name mongodb \
  -p 27017:27017 \
  -v mongodb_data:/data/db \
  mongo:latest

# 验证运行
docker ps | grep mongodb
```

#### 系统安装（Ubuntu/Debian）

```bash
# 导入MongoDB公钥
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# 添加MongoDB仓库
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# 更新包列表
sudo apt update

#<CodeFromFile 
  filePath="code_library/010_Chapter10_Development_Guide/10.1/code_10_1_验证MongoDB连接.py"
  language="python"
  showDesignPrinciples="true"
/>

<!-- 原始代码（保留作为备份）：
```python
# test_mongodb.py
from pymongo import MongoClient

try:
    # 设计原理：使用serverSelectionTimeoutMS设置超时时间
    # 原因：避免在MongoDB未启动时长时间等待，快速失败
    # 替代方案：可以使用connectTimeoutMS，但serverSelectionTimeoutMS更准确
    client = MongoClient('mongodb://localhost:27017', serverSelectionTimeoutMS=2000)
    
    # 设计原理：使用admin.command('ping')进行连接测试
    # 原因：ping命令轻量级，不会产生数据，适合健康检查
    # 替代方案：可以尝试list_database_names()，但ping更快速
    client.admin.command('ping')
    print('✓ MongoDB 连接成功')
except Exception as e:
    print(f'✗ MongoDB 连接失败: {e}')
```
-->，适合健康检查
    # 替代方案：可以尝试list_database_names()，但ping更快速
    client.admin.command('ping')
    print('✓ MongoDB 连接成功')
except Exception as e:
    print(f'✗ MongoDB 连接失败: {e}')
```

**设计说明**：

- **超时设置**：`serverSelectionTimeoutMS=2000` 设置为2秒，避免在MongoDB未启动时长时间阻塞。这个时间足够检测连接，又不会让用户等待太久。
- **健康检查方法**：使用`admin.command('ping')`而不是`list_database_names()`，因为ping命令更轻量级，不会产生额外的数据库操作，适合作为健康检查。
- **错误处理**：捕获所有异常并打印错误信息，帮助用户快速定位问题。

### PostgreSQL配置（可选）

PostgreSQL用于强事务数据存储，如策略元数据、审批流等<CodeFromFile 
  filePath="code_library/010_Chapter10_Development_Guide/10.1/code_10_1_验证PostgreSQL连接.py"
  language="python"
  showDesignPrinciples="true"
/>

<!-- 原始代码（保留作为备份）：
```python
# test_postgres.py
import psycopg<CodeFromFile 
  filePath="code_library/010_Chapter10_Development_Guide/10.1/code_10_1_验证Redis连接.py"
  language="python"
  showDesignPrinciples="true"
/>

<!-- 原始代码（保留作为备份）：
```python
# test_redis.py
import redis

try:
    r = redis.Redis(host='localhost', port=6379, db=0)
    r.ping()
    print('✓ Redis 连接成功')
except Exception as e:
    print(f'✗ Redis 连接失败: {e}')
```
-->连接成功')
    conn.close()
except Exception as e:
    print(f'✗ PostgreSQL 连接失败: {e}')
```
-->s
```

#### 验证PostgreSQL连接

```python
# test_postgres.py
import psycopg2

try:
    conn = psycopg2.connect(
        host='localhost',
        port=5432,
        database='trquant',
        user='trquant',
        password='your_password'
    )
    print('✓ PostgreSQL 连接成功')
    conn.close()
except Exception as e:
    print(f'✗ PostgreSQL 连接失败: {e}')
```

### Redis配置（可选）

Redis用于缓存和队列，如实时行情缓存、任务队列等。

#### Docker方式安装

```bash
# 拉取Redis镜像
docker pull redis:7

# 运行Redis容器
docker run -d \
  --name redis \
  -p 6379:6379 \
  -v redis_data:/data \
  redis:7

# 验证运行
docker ps | grep redis
```

#### 验证Redis连接

```python
# test_redis.py
import redis

try:
    r = redis.Redis(host='localhost', port=6379, db=0)
    r.ping()
    print('✓ Redis 连接成功')
except Exception as e:
    print(f'✗ Redis 连接失败: {e}')
```

<h2 id="section-10-1-4">🔧 10.1.4 开发工具配置</h2>

开发工具配置包括IDE、代码格式化、代码检查和测试框架的配置。

### IDE配置

#### VS Code / Cursor配置

```json
// .vscode/settings.json
{
  "python.defaultInterpreterPath": "${workspaceFolder}/venv/bin/python",
  "python.linting.enabled": true,
  "python.linting.pylintEnabled": true,
  "python.linting.mypyEnabled": true,
  "python.formatting.provider": "black",
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "sourc<CodeFromFile 
  filePath="code_library/010_Chapter10_Development_Guide/10.1/code_10_1_check_python_version.py"
  language="python"
  showDesignPrinciples="true"
/>

<!-- 原始代码（保留作为备份）：
```python
# scripts/verify_environment.py
#!/usr/bin/env python3
"""环境验证脚本"""
import sys
import importlib

def check_python_version():
    """检查Python版本"""
    version = sys.version_info
    if version.major < 3 or (version.major == 3 and version.minor < 11):
        print(f"✗ Python版本过低: {version.major}.{version.minor}")
        print("  要求: Python 3.11+")
        return False
    print(f"✓ Python版本: {version.major}.{version.minor}.{version.micro}")
    return True

def check_package(package_name, import_name=None):
    """检查包是否安装"""
    if import_name is None:
        import_name = package_name
    
    try:
        importlib.import_module(import_name)
        print(f"✓ {package_name} 已安装")
        return True
    except ImportError:
        print(f"✗ {package_name} 未安装")
        return False

def check_database(host, port, db_type):
    """检查数据库连接"""
    try:
        if db_type == 'mongodb':
            from pymongo import MongoClient
            client = MongoClient(f'mongodb://{host}:{port}', serverSelectionTimeoutMS=2000)
            client.admin.command('ping')
            print(f"✓ MongoDB 连接成功 ({host}:{port})")
            return True
        elif db_type == 'postgres':
            import psycopg2
            conn = psycopg2.connect(host=host, port=port, database='trquant', user='trquant', password='')
            conn.close()
            print(f"✓ PostgreSQL 连接成功 ({host}:{port})")
            return True
        elif db_type == 'redis':
            import redis
            r = redis.Redis(host=host, port=port, db=0)
            r.ping()
            print(f"✓ Redis 连接成功 ({host}:{port})")
            return True
    except Exception as e:
        print(f"✗ {db_type.upper()} 连接失败: {e}")
        return False

def main():
    """主函数"""
    print("=" * 50)
    print("TRQuant 环境验证")
    print("=" * 50)
    
    # 检查Python版本
    if not check_python_version():
        sys.exit(1)
    
    print("\n检查核心依赖:")
    packages = [
        ('numpy', 'numpy'),
        ('pandas', 'pandas'),
        ('PyQt6', 'PyQt6'),
        ('fastapi', 'fastapi'),
        ('jqdatasdk', 'jqdata'),
    ]
    
    all_ok = True
    for package_name, import_name in packages:
        if not check_package(package_name, import_name):
            all_ok = False
    
    print("\n检查可选依赖:")
    optional_packages = [
        ('pymongo', 'pymongo'),
        ('psycopg2', 'psycopg2'),
        ('redis', 'redis'),
    ]
    
    for package_name, import_name in optional_packages:
        check_package(package_name, import_name)
    
    print("\n检查数据库连接（可选）:")
    # MongoDB
    try:
        check_database('localhost', 27017, 'mongodb')
    except:
        print("⚠ MongoDB 未配置或未运行")
    
    # PostgreSQL
    try:
        check_database('localhost', 5432, 'postgres')
    except:
        print("⚠ PostgreSQL 未配置或未运行")
    
    # Redis
    try:
        check_database('localhost', 6379, 'redis')
    except:
        print("⚠ Redis 未配置或未运行")
    
    print("\n" + "=" * 50)
    if all_ok:
        print("✓ 环境验证通过")
    else:
        print("✗ 环境验证失败，请检查缺失的依赖")
        sys.exit(1)

if __name__ == '__main__':
    main()
```
-->is'),
    ]
    
    for package_name, import_name in optional_packages:
        check_package(package_name, import_name)
    
    print("\n检查数据库连接（可选）:")
    # MongoDB
    try:
        check_database('localhost', 27017, 'mongodb')
    except:
        print("⚠ MongoDB 未配置或未运行")
    
    # PostgreSQL
    try:
        check_database('localhost', 5432, 'postgres')
    except:
        print("⚠ PostgreSQL 未配置或未运行")
    
    # Redis
    try:
        check_database('localhost', 6379, 'redis')
    except:
        print("⚠ Redis 未配置或未运行")
    
    print("\n" + "=" * 50)
    if all_ok:
        print("✓ 环境验证通过")
    else:
        print("✗ 环境验证失败，请检查缺失的依赖")
        sys.exit(1)

if __name__ == '__main__':
    main()
```

### 运行验证

```bash
# 运行环境验证脚本
python scripts/verify_environment.py

# 或直接运行
python -c "import sys; print(f'Python {sys.version}')"
python -c "import numpy; print(f'NumPy {numpy.__version__}')"
python -c "import pandas; print(f'Pandas {pandas.__version__}')"
```

### 功能测试

```bash
# 测试数据源连接
python -c "from jqdata.client import JQDataClient; print('JQData OK')"

# 测试GUI启动（如果安装了PyQt6）
python TRQuant.py --fast

# 运行单元测试
pytest tests/ -v
```

## 🔗 相关章节

- **10.2 开发原则**：了解开发原则和最佳实践
- **10.3 开发工作流**：了解开发流程和规范
- **第2章：数据源**：了解数据源配置和使用

## 💡 关键要点

1. **Python版本**：必须使用Python 3.11+
2. **虚拟环境**：推荐使用虚拟环境隔离依赖
3. **数据库可选**：MongoDB、PostgreSQL、Redis都是可选的
4. **环境变量**：使用.env文件管理配置
5. **验证脚本**：使用验证脚本确保环境正确

## 🔮 总结与展望

<div class="summary-outlook">
  <h3>本节回顾</h3>
  <p>本节系统介绍了环境搭建，包括Python环境配置、依赖安装、数据库配置、开发工具配置和环境验证。通过理解环境搭建的完整流程，帮助开发者快速搭建开发环境，为系统开发奠定基础。</p>
  
  <h3>下节预告</h3>
  <p>掌握了环境搭建后，下一节将介绍开发原则，包括模块化设计、接口抽象、工作流编排和可追溯性等总体设计原则。通过理解开发原则，帮助开发者掌握系统开发的核心设计理念。</p>
  
  <a href="/ashare-book6/010_Chapter10_Development_Guide/10.2_Development_Principles_CN" class="next-section">
    继续学习：10.2 开发原则 →
  </a>
</div>

> **适用版本**: v1.0.0+  
> **最后更新**: 2025-12-12

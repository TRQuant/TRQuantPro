# TRQuant 项目大小与内容分析报告

**生成时间**: 2025-12-06  
**项目路径**: `/home/taotao/dev/QuantTest/TRQuant`  
**总大小**: 约 **5.9GB** (包含所有文件)

---

## 📊 一、总体概览

### 1.1 目录大小分布（Top 20）

| 目录 | 大小 | 占比 | 说明 |
|------|------|------|------|
| `.backups/` | **3.1GB** | 52.5% | 备份目录（可删除） |
| `venv/` | **994MB** | 16.8% | Python虚拟环境（可重建） |
| `extension/` | **710MB** | 12.0% | VS Code扩展 |
| `docs/` | **137MB** | 2.3% | 文档目录 |
| `core/` | **22MB** | 0.4% | 核心代码 |
| `ptrade_bridge/` | **14MB** | 0.2% | PTrade桥接 |
| `scripts/` | **8.2MB** | 0.1% | 脚本文件 |
| `utils/` | **6.3MB** | 0.1% | 工具函数 |
| `gui/` | **5.5MB** | 0.1% | GUI界面 |
| `results/` | **2.8MB** | <0.1% | 结果文件 |
| `strategies/` | **2.1MB** | <0.1% | 策略文件 |
| `prompts/` | **1.9MB** | <0.1% | 提示词 |
| `bridge_common/` | **1.6MB** | <0.1% | 桥接通用 |
| `quantconnect_bridge/` | **1.2MB** | <0.1% | QuantConnect桥接 |
| `logs/` | **1.2MB** | <0.1% | 日志文件 |
| `markets/` | **1.1MB** | <0.1% | 市场数据 |
| `packaging/` | **1.5MB** | <0.1% | 打包脚本 |
| `dashboard/` | **988KB** | <0.1% | 仪表盘 |
| `qmt_bridge/` | **980KB** | <0.1% | QMT桥接 |
| `data/` | **856KB** | <0.1% | 数据文件 |

### 1.2 文件统计

| 类型 | 文件数 | 总大小 | 说明 |
|------|--------|--------|------|
| **Python (.py)** | 10,711 | ~50MB | 源代码 |
| **Markdown (.md)** | 4,856 | ~3.5MB | 文档 |
| **JSON (.json)** | 18,311 | ~4.9MB | 配置文件 |
| **HTML (.html)** | 497 | ~26MB | 报告/页面 |
| **TypeScript/JS** | 306 | ~2.2MB | 前端代码 |
| **PDF** | 36+ | **1.7GB** | 文档/参考书 |
| **图片** | 130+ | ~67MB | 图片资源 |
| **日志** | 6+ | ~1.2MB | 日志文件 |
| **CSV** | 10+ | ~156KB | 数据文件 |
| **打包文件** | 4+ | ~67MB | .vsix/.tgz |

---

## 📁 二、详细分析

### 2.1 可删除/清理项（节省约 4.1GB）

#### 🔴 高优先级删除项

1. **`.backups/` 目录** - **3.1GB**
   - 包含历史备份
   - 建议：删除或移至项目外部
   - 节省：**3.1GB**

2. **`venv/` 目录** - **994MB**
   - Python虚拟环境，可通过 `requirements.txt` 重建
   - 建议：删除，在目标系统重建
   - 节省：**994MB**

3. **`extension/AShare-manual/node_modules/`** - 约 **200MB+**
   - Node.js依赖，可通过 `npm install` 重建
   - 建议：删除，在目标系统重建
   - 节省：**~200MB**

4. **`extension/AShare-manual/.astro/`** - 已清理
   - Astro构建缓存
   - 状态：已删除

#### 🟡 中优先级清理项

5. **PDF文档重复** - **~500MB**
   - 多个位置存在相同PDF：
     - `docs/` 目录
     - `extension/docs/05_reference_books/`
     - `extension/AShare-manual/docs/`
     - `.backups/` 中的重复
   - 建议：保留一份，删除重复
   - 节省：**~500MB**

6. **Git大对象** - **~738MB**
   - `.git/objects/` 中的大文件（378MB + 360MB）
   - 包含历史中的大文件
   - 建议：使用 `git gc --aggressive` 清理，或考虑重建仓库
   - 节省：**~738MB**

7. **打包文件** - **~67MB**
   - `.vsix`, `.tgz` 文件
   - 建议：删除，可通过构建重新生成
   - 节省：**~67MB**

8. **日志文件** - **~1.2MB**
   - `logs/*.log`
   - 建议：删除或压缩归档
   - 节省：**~1.2MB**

#### 🟢 低优先级清理项

9. **生成报告** - **~26MB**
   - `reports/*.html`（已移至备份）
   - `results/*.html`
   - 建议：保留最新，删除旧报告
   - 节省：**~26MB**

10. **Python缓存** - **~40MB**
    - `__pycache__/` 目录（396个）
    - `.pyc` 文件（10,851个）
    - 建议：删除，运行时自动生成
    - 节省：**~40MB**

11. **临时文件**
    - `.tmp`, `.temp`, `.backup`, `.bak` 文件
    - 建议：删除
    - 节省：**~几MB**

### 2.2 大文件清单（>10MB）

| 文件路径 | 大小 | 类型 | 建议 |
|---------|------|------|------|
| `.git/objects/ea/d588f98898e4fa009462f00188d82644196b6c` | 378MB | Git对象 | 清理Git历史 |
| `.git/objects/f5/fcb9b757f60a4d85276fabf624e65bd20f480f` | 360MB | Git对象 | 清理Git历史 |
| `.backups/.../extension/trquant-cursor-extension-0.1.0.tgz` | 361MB x 3 | 打包文件 | 删除备份 |
| `extension/.../Quantconnect-Writing-Algorithms-Python.pdf` | 40MB x 3 | PDF | 保留一份 |
| `venv/.../libicudata.so.73` | 31MB | 库文件 | 虚拟环境，可删除 |
| `venv/.../libscipy_openblas64_...so` | 24MB | 库文件 | 虚拟环境，可删除 |
| `venv/.../libQt6Gui.so.6` | 11MB | 库文件 | 虚拟环境，可删除 |

### 2.3 代码统计

#### Python代码
- **文件数**: 10,711
- **总行数**: 约 1,582,566 行
- **总大小**: ~50MB
- **主要目录**:
  - `core/`: 22MB
  - `ptrade_bridge/`: 14MB
  - `scripts/`: 8.2MB
  - `utils/`: 6.3MB
  - `gui/`: 5.5MB

#### TypeScript/JavaScript代码
- **文件数**: 306
- **总行数**: 约 378,916 行
- **总大小**: ~2.2MB
- **主要目录**: `extension/src/`

#### Markdown文档
- **文件数**: 4,856
- **总行数**: 约 1,601,802 行
- **总大小**: ~3.5MB
- **最大文件**: `extension/docs/.../cursor_untitled_chat.md` (3.2MB)

---

## 🎯 三、删减建议

### 3.1 必须删除（节省约 4.1GB）

```bash
# 1. 删除备份目录
rm -rf .backups/

# 2. 删除虚拟环境
rm -rf venv/

# 3. 删除node_modules（在extension/AShare-manual/）
rm -rf extension/AShare-manual/node_modules/

# 4. 删除Python缓存
find . -type d -name "__pycache__" -exec rm -rf {} +
find . -type f -name "*.pyc" -delete

# 5. 删除打包文件
find . -name "*.vsix" -o -name "*.tgz" -o -name "*.tar.gz" | grep -v ".backups" | xargs rm -f

# 6. 删除日志
rm -rf logs/*.log

# 7. 清理Git历史（可选，需谨慎）
git gc --aggressive --prune=now
```

### 3.2 建议删除（节省约 500MB）

```bash
# 1. 删除重复PDF（保留一份）
# 建议保留：extension/AShare-manual/docs/ 中的版本
rm -rf docs/*.pdf  # 如果extension中已有
rm -rf extension/docs/05_reference_books/*.pdf  # 如果AShare-manual中已有

# 2. 删除旧报告（保留最新）
find reports/ -name "*.html" -mtime +30 -delete
find results/ -name "*.html" -mtime +30 -delete

# 3. 删除临时文件
find . -name "*.tmp" -o -name "*.temp" -o -name "*.backup" -o -name "*.bak" | xargs rm -f
```

### 3.3 保留项（核心功能）

✅ **必须保留**:
- `core/` - 核心代码
- `config/` - 配置文件
- `strategies/` - 策略模板
- `utils/` - 工具函数
- `scripts/` - 脚本（不含生成物）
- `extension/src/` - 扩展源码
- `extension/package.json` - 扩展配置
- `requirements.txt` - Python依赖
- `extension/requirements.txt` - 扩展依赖
- `docs/` 中的指南和总结文件（.md）
- `README.md` 等文档

✅ **建议保留**:
- `docs/` 中的PDF参考书（保留一份）
- `extension/AShare-manual/` 源码（不含node_modules）
- `Projects/` 和 `ProjectsTest/` 示例项目

---

## 📋 四、清理后预期大小

| 项目 | 当前大小 | 清理后大小 | 节省 |
|------|----------|------------|------|
| 总大小 | 5.9GB | **~1.5GB** | **4.4GB** |
| 核心代码 | ~100MB | ~100MB | 0 |
| 文档 | 137MB | ~100MB | 37MB |
| 扩展 | 710MB | ~200MB | 510MB |

**清理后总大小**: 约 **1.5GB**（不含虚拟环境和node_modules）

---

## 🔧 五、Windows迁移建议

### 5.1 迁移前清理

执行上述删除操作，将项目大小从 **5.9GB** 降至 **~1.5GB**

### 5.2 迁移清单

**必须复制**:
- 所有 `.py` 文件
- 所有 `.md` 文件（指南和总结）
- 所有 `.json` 配置文件
- `requirements.txt`
- `extension/package.json`
- `extension/src/` 目录
- `extension/AShare-manual/src/` 目录（不含node_modules）

**不需要复制**:
- `.backups/`
- `venv/`
- `node_modules/`
- `__pycache__/`
- `*.pyc`
- `*.vsix`, `*.tgz`
- `logs/`
- 旧报告文件

### 5.3 迁移后重建

```powershell
# Windows PowerShell
# 1. 重建Python环境
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt

# 2. 重建扩展依赖
cd extension
npm install
cd AShare-manual
npm install
```

---

## 📝 六、总结

### 6.1 关键发现

1. **备份目录占用最大**（3.1GB，52.5%），可安全删除
2. **虚拟环境占用大**（994MB），可在目标系统重建
3. **PDF文档重复**，可删除重复副本
4. **Git历史包含大文件**，建议清理
5. **核心代码仅约100MB**，非常精简

### 6.2 推荐操作

1. ✅ 删除 `.backups/` 目录
2. ✅ 删除 `venv/` 目录
3. ✅ 删除 `extension/AShare-manual/node_modules/`
4. ✅ 删除Python缓存
5. ✅ 删除打包文件
6. ✅ 清理重复PDF（保留一份）
7. ⚠️ 可选：清理Git历史（需谨慎）

### 6.3 清理后效果

- **大小**: 从 5.9GB → **~1.5GB**（节省 4.4GB，74%）
- **可迁移性**: 大幅提升
- **功能完整性**: 100% 保留

---

**报告生成时间**: 2025-12-06  
**下次更新建议**: 执行清理后重新生成



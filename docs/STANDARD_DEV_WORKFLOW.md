# TRQuant 标准开发流程

## 🔄 开发周期

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1. 规划    │ -> │  2. 开发    │ -> │  3. 测试    │ -> │  4. 记录    │
│  task.create│    │  编码实现    │    │  验证功能    │    │  devlog.add │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
       ^                                                        │
       └────────────────────────────────────────────────────────┘
                            持续迭代
```

## 📝 步骤详解

### 1. 规划阶段
```bash
# 创建任务
task.create(title="功能名称", description="描述", priority="high")

# 检查里程碑
milestone.progress(milestone_id)
```

### 2. 开发阶段
```bash
# 更新任务状态
task.update(task_id, status="in_progress")

# 遇到问题时查询
issue_tracker.quick_debug(error_message)

# 记录经验
experience.add(title="经验标题", content="学到的内容")
```

### 3. 测试阶段
```bash
# 运行测试
pytest tests/ -v

# 端到端测试
# 通过GUI执行9步工作流
```

### 4. 记录阶段
```bash
# 记录开发日志
devlog.add(content="完成了什么", tags=["feature"])

# 如果有新问题
issue_tracker.record_issue(title, description, error_message)

# 记录解决方案
issue_tracker.record_solution(issue_id, description, code_snippet)

# 更新任务状态
task.update(task_id, status="completed")

# Git提交
git commit -m "feat/fix: 描述"
```

## 🔍 Debug流程

```
遇到错误 -> quick_debug(error) -> 有记录? -> 应用解决方案
                                    ↓ 无
                              研究问题 -> 解决 -> record_solution()
```

## 📊 数据存储

| 类型 | 文件 | 说明 |
|------|------|------|
| 任务 | `.trquant/project_data/trquant/tasks.json` | 开发任务 |
| 日志 | `.trquant/project_data/trquant/devlog.json` | 开发记录 |
| 经验 | `.trquant/project_data/trquant/experience.json` | 学习经验 |
| 问题 | `data/issues/known_issues.json` | 已知问题 |
| 方案 | `data/issues/solutions.json` | 解决方案 |
| 模式 | `data/issues/code_patterns.json` | 代码模式 |

---
**版本**: 1.0 | **更新**: 2025-12-17

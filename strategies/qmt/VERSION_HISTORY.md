# TRQuant Weekly Factor Strategy - 版本历史

## 版本管理规则

每次修改策略代码时：
1. **更新主文件版本号**：在文件头部更新版本号和版本历史
2. **创建带时间戳的备份**：`TRQuant_Weekly_Factor_V{版本号}_{时间戳}.py`
3. **更新本文档**：记录版本变更内容

---

## 版本列表

### V4.3 (2026-01-10 01:49:02)

**文件**:
- `TRQuant_Weekly_Factor_V4.py` (主文件)
- `TRQuant_Weekly_Factor_V4.3_20260110_014902.py` (备份)

**变更内容**:
1. ✅ **修复大数据量获取问题**
   - 增强 `get_all_stock_data()` 错误处理
   - 添加数据获取进度日志
   - 处理5404只股票的数据获取超时/失败问题
   - 添加fallback机制（如果全部失败，尝试前1000只）

2. ✅ **增强错误诊断**
   - 在 `handlebar()` 函数中添加try-catch包装
   - 添加详细的错误日志和traceback
   - 改进数据加载失败时的错误提示

3. ✅ **改进日志输出**
   - 显示数据获取进度（"Fetching X stocks..."）
   - 显示成功获取的股票数量（"Retrieved X/Y stocks"）
   - 更清晰的错误信息

**修改位置**:
- 第2行：版本号更新为 V4.3
- 第3-10行：添加V4.3版本历史记录
- 第158-220行：增强 `get_all_stock_data()` 函数
- 第671-690行：增强 `handlebar()` 错误处理
- 第703-710行：增强数据加载错误处理

**问题修复**:
- ✅ 修复5404只股票导致的数据获取失败
- ✅ 添加详细的错误诊断信息
- ✅ 防止静默失败（所有错误都会输出日志）

**测试状态**: 待QMT回测验证

---

### V4.2 (2026-01-10 01:44:31)

**文件**:
- `TRQuant_Weekly_Factor_V4.py` (主文件)
- `TRQuant_Weekly_Factor_V4.2_20260110_014431.py` (备份)

**变更内容**:
1. ✅ **全A股筛选（默认模式）**
   - 将 `STRATEGY_MODE` 默认值改为 `'full'`（全A股）
   - 移除股票采样逻辑，使用全部A股进行因子计算
   - 确保从全市场选股，而不仅仅是HS300

2. ✅ **增强交易逻辑**
   - 改进 `order_shares()` 函数，添加详细的错误日志
   - 确保交易数量是100的倍数（1手=100股）
   - 添加资金和持仓检查，确保交易能正确执行
   - 改进买入/卖出日志输出，显示更详细信息

**修改位置**:
- 第2行：版本号更新为 V4.2
- 第3-15行：添加V4.2版本历史记录
- 第12行：`STRATEGY_MODE = 'full'`（默认全A股）
- 第19行：移除 `SAMPLE_SIZE` 参数
- 第599-607行：移除采样逻辑，使用全部股票
- 第101-133行：增强 `order_shares()` 函数

**关键改进**:
- ✅ 从全A股（~5000只）筛选，不再局限于HS300
- ✅ 交易逻辑更健壮，确保订单能正确执行
- ✅ 详细的交易日志，便于调试

**测试状态**: 待QMT回测验证

---

### V4.1 (2026-01-10 01:42:20)

**文件**:
- `TRQuant_Weekly_Factor_V4.py` (主文件)
- `TRQuant_Weekly_Factor_V4.1_20260110_014220.py` (备份)

**变更内容**:
1. ✅ **添加调试日志**
   - 在 `handlebar()` 函数中添加每10个bar的调试输出
   - 用于确认 `handlebar` 是否被QMT正常调用
   - 输出格式：`[Bar {d}] {date_str} - Warmup: {bool}, Rebalance day: {bool}`

2. ✅ **过滤无效股票代码**
   - 在 `init()` 函数中，设置universe前过滤已知退市股票
   - 移除：`601989.SH`, `600837.SH`
   - 解决QMT警告：`[系统][WARNING][set_universe]无效股票代码`

**修改位置**:
- 第2行：版本号更新为 V4.1
- 第3-12行：添加版本历史记录
- 第599-613行：添加无效股票过滤逻辑
- 第652-659行：添加调试日志

**测试状态**: 待QMT回测验证

---

### V4.0 (2026-01-10)

**文件**: `TRQuant_Weekly_Factor_V4.py`

**变更内容**:
1. ✅ **对齐BulletTrade版本**
   - 7个验证因子计算逻辑完全对齐
   - 因子评分函数与BulletTrade版本一致
   - 筛选逻辑与BulletTrade版本一致

2. ✅ **QMT API集成**
   - 使用 `ContextInfo.get_financial_data()` 获取财务数据
   - 使用 `ContextInfo.get_history_data()` 获取价格数据
   - 添加回退机制（财务数据获取失败时使用估算）

3. ✅ **参数对齐**
   - `MIN_TOTAL_SCORE = 30.0` (对齐BulletTrade)
   - `MAX_MARKET_CAP = 200.0` (对齐BulletTrade)
   - 添加完整的6个筛选条件

**核心功能**:
- 周频调仓（每5个交易日）
- HS300或全A股模式
- 7因子综合评分
- 完整的轮动逻辑

**测试状态**: 已通过代码验证，待QMT回测

---

## 版本命名规范

### 主文件
```
TRQuant_Weekly_Factor_V{主版本号}.py
```

### 备份文件
```
TRQuant_Weekly_Factor_V{主版本号}.{次版本号}_{YYYYMMDD_HHMMSS}.py
```

**示例**:
- 主文件：`TRQuant_Weekly_Factor_V4.py`
- 备份：`TRQuant_Weekly_Factor_V4.1_20260110_014220.py`

---

## 版本号规则

- **主版本号**：重大架构变更或算法逻辑重构
- **次版本号**：功能增强、API适配、参数调整
- **修订号**：Bug修复、代码优化、日志改进

当前版本：**V4.3**

---

## 快速查找

| 版本 | 日期 | 主要变更 | 文件 |
|------|------|----------|------|
| V4.3 | 2026-01-10 | 修复大数据量获取 + 错误诊断增强 | `TRQuant_Weekly_Factor_V4.py` |
| V4.2 | 2026-01-10 | 全A股筛选 + 交易逻辑增强 | `TRQuant_Weekly_Factor_V4.py` |
| V4.1 | 2026-01-10 | 调试日志 + 无效股票过滤 | `TRQuant_Weekly_Factor_V4.py` |
| V4.0 | 2026-01-10 | 初始版本，对齐BulletTrade | `TRQuant_Weekly_Factor_V4.py` |

---

## 使用建议

1. **开发测试**：使用主文件 `TRQuant_Weekly_Factor_V4.py`
2. **版本追溯**：查看备份文件 `TRQuant_Weekly_Factor_V4.X_YYYYMMDD_HHMMSS.py`
3. **回滚**：如需回滚到旧版本，复制对应的备份文件覆盖主文件

---

**最后更新**: 2026-01-10 01:49:02
